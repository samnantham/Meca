{
    "private": true,
    "scripts": {
        "heroku-postbuild": "ng build --prod",
    },
    "devDependencies": {
        "axios": "^0.19",
        "bootstrap": "^3.0.0",
        "cross-env": "^5.1",
        "jquery": "^3.2"
    }
}
